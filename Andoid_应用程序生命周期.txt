Application对应一个进程，当该进程的所有Activity都退光了的时候，该进程也不会立即结束.

比如开一个线程在后台打Log,然后退出所有Activity，那么该进程还是会在后台运行. 很奇怪的设计！也就是说并不存在一个主线程.

如果进程在Manifest.xml里写了Receiver组建,那么在收到广播时，会调用相应的OnReceive方法(对象应该是由Android创建).如果OnReceive方法比较耗时，那么最好是能起一个服务来进行工作，而不是起一个线程.因为有服务的进程比有线程的进程更不容易被Android关掉，也就是进程的优先级较高.